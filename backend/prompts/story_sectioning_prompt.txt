Split the story below into logical page-sized sections.

Return JSON only, matching the schema.
Field rules:
- sections: 3 to 20 entries.
- Keep narrative flow with natural scene breaks.
- Preserve full story meaning across the sections.
- Do not add commentary, labels, or markdown.

STORY:
{full_story_text}
